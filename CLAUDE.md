# One-Person Brand — agent instructions

Read `AGENTS.md` in this folder and follow it exactly. It is your operating manual for this repository: how to bootstrap the human's second brain on first contact, and the non-negotiable session rules (read `context.md` first; commit and push every change; write decisions back before ending) for every session after.

`.claude/agents/` holds this vault's **critic bench** — `art-director`, `copy-editor`, `first-principles`, and `sceptic`. Nothing visual, nothing public-facing, and no committed plan is final until the relevant critic has reviewed it — on every revision, not just the first draft. See the *critic bench* section of `AGENTS.md` for how to run them and how to keep them sharp. Never move or delete `.claude/` when tidying the vault.
